function citiesOnly(arrCities) {
  var cities = arr1.map(function (city) {
    return arr1.city;
  });

  return cities;
}

function upperCasingStates(arrStates) {
  var states = arrStates.map(function (state) {
    return arrStates[state].string.charAt(0).toUpperCase;
  });
  return states;
}
